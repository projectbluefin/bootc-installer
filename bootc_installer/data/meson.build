pkgdatadir = join_paths(get_option('prefix'), get_option('datadir'), meson.project_name())
datadir = join_paths(pkgdatadir, 'bootc_installer/data')

install_data(
  'credits.json',
  'dinosaurs.json',
  'tracks.json',
  install_dir: datadir,
)
